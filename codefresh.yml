version: '1.0'
steps:
  main_clone:
    title: 'Cloning main repository...'
    type: git-clone
    repo: '${{CF_REPO_OWNER}}/${{CF_REPO_NAME}}'
    revision: '${{CF_REVISION}}'
    git: github
  free: 
    image: alpine
    commands:
      - printenv | grep CF
      - echo "VAR1=${{VAR1}}"
      - echo "VAR2=$VAR2"
      - pwd
